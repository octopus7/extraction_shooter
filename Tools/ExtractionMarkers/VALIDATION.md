# Extraction markers validation

Source meshes: portable beacon 132 triangles / 2 material slots; inclined sign 60 / 1; reused emergency light 12 / 2. Total 204 triangles and 5 slots. Beacon bounds are 60 × 68.5 × 55 cm including handle; sign bounds 68 × 90 × 29 cm; light 40 × 12 × 20 cm. The sign panel slopes 20 degrees and its symbol points +X. No bevel modifiers, subdivisions or Nanite are used.

Source and all three FBXs pass signed bounds, identity pivots/transforms, finite UV0/UV1, nonzero geometric/UV triangle area, outward closed shells, material assignment and exact source/reloaded position/UV signatures. Touching closed shells are intentionally separate; their internal mating caps are not merged. The original light FBX and five required UE dependency packages are byte-identical to `8bb53750`. The existing sign body is transformed rather than duplicated as a second vertical sign design.

One optional analytic beacon box (60 × 60 × 55 cm, excluding its small handle) is saved in the UE mesh. No collision is authored for the sign or light. All three sample components have collision, overlap events and navigation influence disabled. Fresh editor sphere sweeps through both center axes and all props pass; the floor trace hits the dedicated review ground. All props sit outside the explicit 300 cm sample radius. Read the final report for precise signed bounds/clearance values.

The new ImageGen atlas is 1024² sRGB. Existing 2048² steel atlas and 1024² linear dirt mask are reused. New surface and existing steel each have two reachable texture sample nodes; green and amber emission have zero. Two constant material instances switch emission off for preview/optional reuse. Ordinary surfaces are Opaque. Added dirt is controlled with the shared mask and CPD0..3; no broad decals, normal maps or roughness maps were added. No prop Point/Spot/Rect lights were added. The review map has one directional light and one skylight. These numbers describe meshes and material graphs, not FPS or measured draw calls.

UE 5.7.4 editor build succeeds. Initial null-RHI texture import hit a Slate initialization assertion, so final imports use the full editor. The existing `NE_PostProcess` typed-element ensure can appear during project startup; the final full-editor asset and map audits exit 0 and pass. They verify persisted bounds, triangles, slots, analytic collision box, UV channels/lightmap index 2, texture sizes, material graphs, original extraction BP/ring/smoke references and actual player BP camera settings (1500 cm arm, pitch −88°, yaw 0°, FOV 70°).

The original ring is 192 triangles. Existing runtime smoke is `/Game/FX/NS_ExtractionSmoke`; the legacy smoke asset also loads. Static editor captures do not advance this smoke. Separate `UE_SimulatedSmoke_*` images use Simulate in Editor with the existing effects. The dedicated sample alone uses GameModeBase and an empty extraction target to prevent travel. This is visual SIE verification, not an end-to-end gameplay extraction test. The original maps, BP, source rules, save schema and quest data remain unchanged.

The first east-side placement was obscured by the existing plume. The final source/sample assembly is south/upwind, preserving the original smoke asset and parameters. The large arrow faces the beacon and extraction center. Native-scale topdown footage is intentionally included: the sign is readable but remains a small environmental prop at a 1500 cm camera distance; smoke can still occlude scene regions as it develops.

Evidence beside `ExtractionMarkers.blend`: `source_validation.json`, `unreal_import_validation.json`, `unreal_reload_validation.json`, `unreal_scene_validation.json`, `unreal_scene_reload_validation.json`, `unreal_smoke_preview.json`, `delivery_validation.json`. `Review.html` distinguishes actual renders from the ImageGen concept reference. Prompts are retained verbatim in the tool directory, generated original PNGs in References/Textures. Source commit, visual placement correction, UE assets plus one-off generators, and immediate generator removal are separate local commits. No remote push.

Final south/upwind bounds clear the sample disk by 60 cm (beacon), 166 cm (sign), and 96.38 cm (light). Final SIE screenshots directly show the preserved plume/ring alongside all three readable props. The source is `4a1b45f8`, placement correction `f98cf3f7`. The final fresh full-editor combined asset/scene audit exits 0; both reports explicitly confirm GameModeBase and an empty travel target on the sample alone. Delivery audit passes 13 runtime packages and nine byte-identical selectively reused files.
