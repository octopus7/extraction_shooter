# Expansion validation evidence

Final measured inventory: 18 expansion meshes, 476 triangles, 20 material slots, 16 analytic collision boxes. With the six unchanged base meshes: 24 unique meshes / 636 triangles. Combined sample: 96 actors / 1,556 mesh triangles, six fixture rect lights and two review softboxes. No ceiling, beam or pillar is present.

Blender 4.5.12 build and fresh `.blend`/24-FBX validation exit 0. The source report checks manifold geometry, signed volume/outward winding, nonzero geometry/UV triangle area, signed bounds, origin/scale/rotation, two authored UV layers, material slots and UCX hull inventory. Structural collision boxes have no positive-volume overlaps. Pipe/cap/elbow and duct/corner socket position errors are exactly 0m. Independent frame vertices reuse the base doorway coordinates. Base door sweep minimum jamb clearance is 1.8368cm, with 2cm vertical clearance. The new frame uses the same opening and −68° leaf pose. Window frame clearance is 2mm; pane collision intentionally blocks the aperture.

The review renders were visually inspected. Corrections during review included the new atlas connection, pipe placement below the window, door swing direction, module thumbnail framing, and UE-specific sign UV handedness. The final exported sign has a documented U-coordinate compensation; the source Blender sign remains readable. Grating/vent recesses are opaque painted shading, not perforated geometry. Standard light and emergency fixture surfaces face the interior wall side; no ceiling installation is required.

UE5.7.4 editor build succeeded (92.48s). Final asset import uses a full editor and exits 0 after all assertions. A PNG Interchange import through an initial UI-less commandlet hit a Slate `CurrentApplication.IsValid` assertion (exit 3); switching the importer to a normal editor resolved that failure. An initial sandbox attempt could not launch Zen; the final runs use normal user-cache permissions.

Fresh asset commandlets independently pass all 24 mesh assertions but return exit 1 due to the pre-existing `NE_PostProcess` typed-element registry ensure. This engine error is not called a process success. Reports verify triangle counts, signed bounds (<0.1cm tolerance), slots, collision type/count and every box's center/extents, non-Nanite state, authored UV channels and lightmap UV2 build destination. The service atlas is 2048² sRGB. Structural/service surfaces are Opaque; the glass slot is Translucent, opacity .22 without refraction. Mesh UV counts returned by the editor subsystem describe source channels; generated lightmap destination is recorded separately.

Full-editor saved-map validation waits 30 Slate ticks for Chaos registration. It matches every mesh reference, location/yaw/scale and four CPD values against the manifest, checks camera settings against the actual player Blueprint, and runs ten simple collision rays: base wall blocks, base/new doorway passes, frame jamb blocks, floor/grating blocks, window pane/sill block, low partition blocks and a ray above it passes. No PIE/game instance or gameplay save is started. Screenshot callback reentrancy was corrected before the final captures. All final camera outputs are native UE screenshots, not edited Blender images.

The native top-down camera is 1500cm / pitch −88° / yaw 0° / FOV70. Walls retain full height. Floor grating, low partition and the emissive emergency fixture are visible; vertical glass, wall signage, pipe and duct details become narrow or occluded near walls. The frontal sign is readable in the UE close view, not claimed readable from the steep gameplay camera. Outside the isolated test structures the UE scene is unlit black; Blender uses a neutral studio world. UE captures are editor previews, not PIE or FPS measurements.

Ordinary surfaces use two texture reads (atlas + common dirt); constant glass and amber use zero. Three runtime images are shared across the combined kit: existing 2048² atlas, existing 1024² linear mask, and one new 2048² service atlas. Three new materials are added; base materials are reused. No broad decals, new normal maps, per-module texture sets, hidden support geometry, complex-as-simple collision or claimed FPS gain.

Evidence files beside the `.blend`: `model_manifest.json`, `source_validation.json`, `unreal_import_validation.json`, `unreal_reload_validation.json`, `unreal_map_validation.json`, `unreal_map_reload_validation.json`, and cleanup report. The initial source commit is `4f8caf5d`; follow-up `7c7c9b8c` corrects sign handedness and thumbnail framing. The UE asset commit includes its one-off importer/map/capture scripts. The next commit removes all those generators, their entry points and import-only runner modes; it retains only the reusable Blender tools and read-only UE audits. Base content must remain byte-identical to `c2b70edf`. No remote push.

Cleanup validation after asset commit `481528e5`: the one-off importer, map builder, camera capture, two generation drivers and import/create runner modes are removed. Fresh read-only asset assertions pass with the existing Niagara process exit 1; fresh map/physics assertions pass with process exit 0. Reachable material graph nodes independently confirm ServiceSurface has exactly two TextureSample nodes referencing the shared atlas/mask, and Amber/Glass have zero. Glass opacity .22 and absence of a refraction input are asserted. The compiled shader resource texture list is not used in null-RHI mode; the persisted graph texture references are inspected directly.

`audit_cleanup.py 481528e5` passes: 23 runtime packages, 18 FBXs/18 UE meshes, three new materials, one new texture, one map, all five reports passed, 54 gallery links present, all three base paths byte-identical to `c2b70edf`, and all expansion runtime packages unchanged from the asset commit after cleanup/reload. SHA256 values are recorded in `cleanup_validation.json`. The remaining validator and runner sources contain no import, actor creation, package save or screenshot entry points.
